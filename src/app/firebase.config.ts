// src/app/firebase.config.ts
// import { FirebaseOptions } from '@angular/fire/app'; // Commented out Firebase
// import { initializeApp } from '@angular/fire/app'; // Commented out Firebase
// import { getAuth, provideAuth } from '@angular/fire/auth'; // Commented out Firebase
// import { getMessaging, provideMessaging } from '@angular/fire/messaging'; // Commented out Firebase
// import { getFirestore, provideFirestore } from '@angular/fire/firestore'; // Commented out Firebase
// import { environment } from '../environments/environment'; // Commented out Firebase

// Initialize Firebase app
// export function initializeFirebaseApp() { // Commented out Firebase
//   return initializeApp(environment.firebase); // Commented out Firebase
// } // Commented out Firebase

// Export Firebase providers
export const firebaseProviders = [
  // provideAuth(() => getAuth()), // Commented out Firebase
  // provideMessaging(() => getMessaging()), // Commented out Firebase
  // provideFirestore(() => getFirestore()), // Commented out Firebase
];
