import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onemorebattery.voidbrain',
  appName: 'one-more-battery',
  webDir: 'www',
};

export default config;
