# Use an official Node.js runtime as a parent image
FROM node:23

# Set the working directory in the container
WORKDIR /app

# Copy package.json and package-lock.json to the working directory
COPY package*.json ./

# Install any needed packages specified in package.json
# Using --legacy-peer-deps to avoid potential dependency conflicts
RUN npm install --legacy-peer-deps

# Copy the rest of the application's source code from your host to your image filesystem.
COPY . .

# Make port 4200 available to the world outside this container
EXPOSE 4200

# Run the app when the container launches
# --host 0.0.0.0 is crucial to allow access from outside the container
# --disable-host-check is often needed when running in a container
CMD ["npm", "start", "--", "--host", "0.0.0.0", "--disable-host-check"]
